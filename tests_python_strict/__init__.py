# PyDead-BIB Test Suite 💀🦈
# Tipado Implícito Estricto — Sin conversión automática
# Cada tipo respeta sus bits

"""
Reglas de tests:
- Todo valor tiene tipo inferido en tiempo de compilación
- int + float = ERROR (sin excepciones)
- Conversiones deben ser EXPLÍCITAS
- Sin undefined behavior en runtime (detectado en compile-time)
"""
