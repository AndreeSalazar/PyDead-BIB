# NN — Runtime 2.0 💀🦈

Capas de red neuronal compiladas nativas.

- `Linear` — fully connected layer con Kaiming init
- `MLP` — multi-layer perceptron con ReLU entre capas
- Forward pass usa matmul AVX2

## Estado: ✅ Implementado
